// Runtime settings for the game client. In local dev this stays empty and the Vite
// variables (or localhost defaults) apply. In production the match server replaces
// this file with the real lobby address, so one build works on any host.
window.POTLOCK_CONFIG = window.POTLOCK_CONFIG || {};
